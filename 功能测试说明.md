# Furina Agent 0.1.0 功能测试说明

日期：2026-09-23。对象：独立无模型版本，不是旧五形象桌宠的全量重新验收。

## 已实际执行的检查

| 项目 | 方法与结果 |
|---|---|
| 单元测试 | 80 / 80 通过。涵盖会话、工具限制、DeepSeek 分片、文档解析/检索/加密、导出、知识库、提醒、备份、GLB 导入，以及新增连接适配 |
| 无模型启动 | Electron 实际运行，无角色窗口，无缺模型启动依赖 |
| DeepSeek / OpenAI 聊天 | 本地模拟服务验证主进程、IPC、流式显示、停止、重新生成、模式选择和密钥隔离；未使用真实账户计费 |
| 会话切换 | 实际应用中的 IPC 切换并检查历史保留；不等同于所有操作顺序均无 Bug |
| 窗口布局 | 1180×760、960×620 实际窗口截图检查；没有横向溢出 |
| GLB 查看器 | 开发版实际渲染自建三角形测试 GLB，检查旋转查看器加载和移除副本不删除源文件 |
| Codex 协议 | 模拟进程验证初始化、独立登录目录、文本流和取消；本机官方 CLI 0.145.0 已实际启动并读取未登录状态 |
| 最终安装版 | 已实际运行 NSIS 安装器，再从安装目录启动；19 项检查通过，含无模型启动、DeepSeek/OpenAI 模拟回复、Codex 实际未登录握手和 GLB 加载。安装后 app.asar 与打包版本 SHA-256 相同 |
| 发布资源检查 | 检查 app.asar 的 2627 个条目，角色模型、武器、旧资产目录、个人状态文件及疑似真实 Key 均未发现；四个 Three.js 辅助资源存在 |

## 失败记录与修复

1. 首轮回归有 1 项失败，原因是改动状态提示文字后旧断言不匹配；改回兼容提示，重新测试通过。
2. 首轮截图出现 `UnknownVizError`。移除仅旧透明桌宠需要的呈现设置，并在窗口稳定后采集；后续开发版截图成功。
3. 首轮安装资源测试发现 GLB 查看器缺少 `SkeletonUtils.js`。补入依赖后，打包目录中的应用复测通过 18 项检查，包括 GLB 实际渲染。保留首轮失败记录，不把失败写成从未发生。

## 未完成真实端到端验收的项目

- Codex 真实账户的浏览器登录、账户模型列表和付费/订阅回复。当前只验证了本机 CLI 握手及模拟协议，不把它写成真实账号聊天通过。
- 本版的真实 OpenAI API、真实 DeepSeek 网页搜索、真实额度/限流行为。单元测试及模拟服务不能证明外部服务始终可用。
- 麦克风录音、扬声器听感、各机器的中文语音组件；保留了原功能，但本轮没有真人语音测试。
- 全部外部角色模型、蒙皮/材质/动画组合及不同 GPU。一个测试 GLB 能显示，不代表任意用户模型均兼容。
- 多屏、不同 DPI、休眠、杀进程、所有快捷键冲突、全量长时间压力测试和外部更新服务器。

首次公开按**预览版**标注。不宣称“全部功能无 Bug”。旧本地五套模型、武器、立绘和正式桌宠未被本轮替换；发布物不应包含它们。

最终安装器为 `Furina-Agent-0.1.0-x64-Setup.exe`，120,007,174 字节，SHA-256：

`808d74386ad202d797523e82c7b6c12dbd76e3329e80a9737401e1638706ba74`

80 项完整回归后，针对最后的连接与资源改动又执行了 29 项相关测试，全部通过。安装版的 19 项是自动驱动真实 Electron 窗口与 IPC 的冒烟测试，不是 19 项真人操作验收。GLB 实际渲染使用自建静态三角形，不冒充真实角色的动画美术验收。

## 用户可以怎样复查

安装后先不添加模型，配置一种自己的 AI 连接，连续建立和切换两个任务，分别测试发送、停止、重新生成、退出和重启。再导入一个文字文档，核对引用对应原文。最后添加自己的 GLB，检查静态姿势、自带动画、旋转和移除。

Codex 首版只用于聊天与文档，不支持系统工具及网页搜索；未安装 CLI、未登录、无模型权限或无网络时应明确报错，而不是自动使用其他账户。不要把这些连接条件当作已经通过的测试。

Windows 安装器暂未正式签名，可能有“未知发布者 / SmartScreen”提示。

