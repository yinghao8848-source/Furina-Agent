# Furina Agent 0.1.0 功能与测试说明

适用版本：Windows x64 独立 Agent 预览版。测试日期：2026-09-23。

## 软件功能

- AI 连接：支持 DeepSeek API、OpenAI API，以及实验性的 Codex 账号接入。用户使用自己的密钥或账号。
- 对话：新建和切换任务、流式回复、停止生成、重新生成、历史记录和显式记忆。
- 文档阅读：读取 TXT、MD、JSON、CSV、LOG、带文字层的 PDF 和 DOCX，支持问答、总结和来源引用。
- 本地知识库：选择文件夹建立索引，在不同任务中检索其中的文字资料。
- 联网搜索：DeepSeek 和 OpenAI API 模式可使用；Codex 模式暂不支持。
- 日程与导出：待办、提醒、倒计时；单条回答和完整任务可导出 Markdown、DOCX、PDF。
- 本地工具：备份与恢复、诊断信息导出、全局快捷入口。
- 语音：使用 Windows 已安装的语音组件进行输入和朗读。
- 外观：浅色和深色主题、自选背景、界面缩放。
- 可选模型查看器：自行导入 GLB，旋转、缩放、选择自带动画、暂停和复位视角。不附带角色模型，也不会自动生成桌宠动作。

Codex 接入目前用于聊天、文档和知识库问答、总结，不支持系统工具或通过对话写入记忆。

## 测试了什么

| 测试项目 | 测试方式与结果 |
|---|---|
| 安装与启动 | 实际安装并启动软件，不添加角色模型也能进入工作台。通过。 |
| 聊天流程 | 使用本地模拟服务检查 DeepSeek、OpenAI 的逐步回复、停止、重新生成和模式选择。通过；本轮未使用真实在线账号回复。 |
| 会话切换 | 在实际应用中自动切换任务并检查历史记录。通过。 |
| 连接切换 | 检查不同服务的密钥独立保存，切换连接后保留原配置。通过。 |
| Codex 连接 | 实际启动本机 Codex CLI，读取未登录状态；用模拟服务检查回复与取消流程。通过；未完成真实账号登录和聊天测试。 |
| 文档与知识库 | 自动测试检查文字解析、片段检索、引用校验和加密保存。通过。 |
| 提醒、导出与备份 | 自动测试检查提醒逻辑、导出内容和备份处理。通过；系统通知及各类异常环境仍待实测。 |
| 工具权限 | 自动测试检查允许调用的工具和调用次数限制。通过。 |
| 窗口布局 | 检查 1180×760、960×620 两种实际窗口尺寸，没有发现横向溢出。 |
| GLB 查看器 | 用自建静态测试模型检查加载、显示和移除；移除导入副本不影响源文件。通过；未完成外部角色及动画兼容性测试。 |
| 安装版整体流程 | 在实际安装的软件中自动检查启动、聊天、连接切换、窗口布局和模型加载。通过。 |

以上包含自动测试和模拟服务测试，不全部是真人逐项操作测试。
