# Furina Agent 使用说明

适用：0.1.0 无内置模型预览版，Windows x64。

## 1. 安装与启动

1. 在 GitHub 仓库进入 **Releases**，展开 **Assets**。
2. 下载 `Furina-Agent-0.1.0-x64-Setup.exe`。`Source code.zip` 只包含仓库说明，不是软件。
3. 运行安装器，选安装位置，完成后打开桌面或开始菜单中的 **Furina Agent**。
4. 首次运行没有角色模型，也没有可共用的 AI 账户。先进入 **设置 → 模型连接**。

本版与旧 **Furina Desktop Pet** 使用不同的程序身份和数据目录，不会自动导入旧软件中的模型、密钥或历史。点击窗口关闭按钮会收起操作台；彻底退出请使用托盘菜单的“退出”。

## 2. 选择 AI 连接

### DeepSeek

选择 **DeepSeek API**，粘贴自己的 Key，点击“验证并保存”。普通模式使用 V4 Flash；深度思考使用 V4 Pro。可按需打开输入框旁的“联网搜索”。调用费用由你的 API 账户承担。

### OpenAI API

选择 **OpenAI API**，填写自己的 Key，点击“验证并保存”。模型 ID 默认 `gpt-5.4`，可改成账户有权限使用、支持 Responses API 文本及工具功能的模型。模型名称保存不会自动验证模型访问权限；无权限时会在发送请求后报错。

API 按 API 账户计费，不能用 ChatGPT 订阅代替 API Key。DeepSeek 与 OpenAI 密钥分开保存，切换连接不会把其中一个密钥发给另一个服务。参考 [OpenAI Responses 文档](https://developers.openai.com/api/docs/guides/streaming-responses)。

### Codex 账户（预览接入）

1. 根据 [Codex CLI 官方说明](https://learn.chatgpt.com/docs/codex/cli) 安装官方 Windows CLI。本版本对接 0.145 及之后的 App Server 协议；不是把 Codex 桌面应用嵌入软件。
2. 在模型连接里选择 **Codex 账户**，点击“检测 CLI”。未找到时，使用“选择 codex.exe”选择官方 CLI 的可执行文件，不要选择 `.cmd`、`.ps1` 或桌面快捷方式。
3. 点击“登录账户”，在打开的官方页面完成登录。登录由 Codex App Server 管理，Furina Agent 使用独立登录区；你原先在 Codex 中的登录不会自动复制过来，也不会被退出。
4. 返回软件，等连接状态更新。必要时再点“检测 CLI”。可以“刷新可用模型”，从当前账户支持的模型中选择；也可以保持“Codex 默认模型”。
5. “退出此应用登录”只退出这套独立登录区。账号验证页面需要你自己操作。

Codex 首版支持聊天、读取已确认导入的文档片段和全文总结，不执行命令，不调用本软件的记忆写入、日程或系统操作工具，不提供网页搜索。已有显式记忆仍会作为上下文供回答参考；日程、记忆删除等界面功能仍可手动使用。

这是 [Codex App Server](https://learn.chatgpt.com/docs/app-server) 接入，不是订阅共享服务。账户支持的模型、配额和网络可达性仍由官方服务决定。

## 3. 自行添加本地模型

**不用改代码，不用拆安装包。**

1. 准备你自己有权使用的 **GLB 2.0** 文件，单个不超过 **128 MiB**。
2. 打开 **设置 → 本地模型 → 添加 GLB**，选择文件。
3. 导入成功后点击 **打开模型**。
4. 在模型窗口中：左键拖动旋转，滚轮缩放，右键平移；“自带动画”下拉框切换文件原有动画，“暂停”停止播放，“复位视角”重新居中。
5. 更换模型时再次“添加 GLB”。移除时点“移除副本”；只移除应用管理的副本，不删除你选择的原始文件。

首版每次管理一个模型，重启后保留。它显示在独立的 3D 查看窗口中，不是旧版会沿桌面走动的透明桌宠。

### 文件需要满足什么条件

- 网格、材质、贴图必须打包在同一个 GLB 中，不能依赖旁边的图片文件、网络贴图或其他本地路径。
- 不使用 Draco、Meshopt、KTX2 压缩；不支持外部加载插件。
- 有动画就显示动画列表；没有动画可以静态观看，不会自动生成走路、比心、坐姿或表情。
- 支持常规 glTF 材质。MMD 着色器、Blender 自定义节点、物理模拟等不保证在 GLB 中保持原样。

已有 `.pmx`、`.fbx`、`.blend` 不能直接改扩展名。请先在能正确打开该模型的建模软件中转换为 GLB，并检查材质、骨骼和动画是否导出完整。若使用 Blender，导出时选 glTF 2.0 的 **GLB / Binary** 格式，并关闭上述压缩；原格式是否能导入取决于软件本身及所需插件。本应用不负责自动转换或修补骨骼。

### 常见问题

| 现象 | 处理 |
|---|---|
| 提示引用外部文件 | 重新导出为贴图内嵌的单文件 GLB |
| 提示压缩不支持 | 关闭 Draco / Meshopt / KTX2 后重新导出 |
| 模型不动 | 检查文件是否真的包含动画，再从下拉框选择；静态模型不会自动动起来 |
| 材质不对或骨骼扭曲 | 回到原软件检查 glTF 材质和动画烘焙；仅导入无法修复源模型 |
| 文件太大 | 在建模软件中压缩贴图分辨率、清理无用网格，再导出，不要仅压成 ZIP |
| 模型无法显示 | 更换或移除副本；聊天仍然可以使用 |

导入模型只在本机存放，不传给 DeepSeek 或 OpenAI，也不会发布到 GitHub。请不要把自己的账户数据、私人文档或整个应用数据目录当成“模型包”发给别人。

## 4. 聊天与文档

左侧创建、切换和管理任务，中间输入问题。发送中可停止，最后一条回答可重新生成。切换 AI 服务前应先停止当前回复。

“添加文件”支持文本、PDF 文字层和 DOCX。导入会展示隐私说明；原始二进制不上传，但提问时会把检索到的必要文字发送给当前 AI 服务。全文总结会额外确认预计请求数。扫描 PDF 和图片不做 OCR。

知识库由你手动选择文件夹并确认；索引本地保存，跨任务启用后可检索。不要把文件内容中出现的指令视为软件授权。单条回答或整个任务可导出 MD、DOCX、PDF，不要求安装 Office 或 LibreOffice。

## 5. 日程、设置与数据

日程可手动新建待办、提醒、倒计时。完全退出应用后不在后台发提醒；下次启动处理逾期项。语音使用 Windows 已安装的识别器和语音包，缺失时仍可文字输入。

设置可调整主题、背景和界面缩放，管理快捷键、连接、备份与诊断。默认 `Ctrl+Alt+Space` 打开快速对话，`Ctrl+Alt+Shift+F` 唤出操作台；冲突时请换组合键。

会话与本地服务数据留在本机。文档索引加密保存；API Key 使用 Windows 安全存储，系统加密不可用时只在本次运行使用。Codex 登录由 CLI 在本应用独立目录中管理。便携备份不带 API Key、Codex 登录、导入 GLB 或原始文件；恢复后请重新配置连接和本地模型。

自动更新通道暂未启用。下载新版本请使用仓库 Releases。遇到问题先查看 [功能测试说明](功能测试说明.md)，反馈时可写明版本、操作步骤和现象；不要发送 Key 或登录令牌。

